# ПРОГРАММНАЯ РЕАЛИЗАЦИЯ - ДЕТАЛИ ДЛЯ ПРЕЗЕНТАЦИИ

## 📊 ТЕХНИЧЕСКИЕ ХАРАКТЕРИСТИКИ ПО

### Объем кода (KLOC)

| Метрика | Значение |
|---------|----------|
| **API клиент** (`src/api/hh_api_client.py`) | 1,153 строки |
| **Модули анализа** (`analysis_modules/`) | 1,666 строк (11 файлов) |
| **База данных** (`src/database/db_manager.py`) | 917 строк |
| **Анализ данных** (`src/analysis/`) | ~800 строк |
| **Визуализация** (встроена в модули) | ~400 строк |
| **Утилиты и скрипты** | ~500 строк |
| **Прочие модули** | ~300 строк |

**Итого:** ~5,700 строк = **~5.7 KLOC** (основной код, без зависимостей)

---

## 🧪 ПОКРЫТИЕ ТЕСТАМИ

| Компонент | Покрытие | Статус |
|-----------|----------|--------|
| API клиент | ~30% | ⚠️ Частичное |
| База данных | ~40% | ⚠️ Частичное |
| Анализ данных | ~20% | ⚠️ Частичное |
| **Общее покрытие** | **~25%** | ⚠️ Требует улучшения |

**Существующие тесты:**
- `src/api/test_api_client.py` - Тесты API
- `src/database/test_database.py` - Тесты БД

**Целевое покрытие:** 70%+

---

## 🤖 АВТОМАТИЗАЦИЯ ТЕСТИРОВАНИЯ

**Инструменты:**
- ✅ `pytest` - Фреймворк тестирования
- ✅ `unittest` - Встроенный фреймворк
- ⚠️ `coverage.py` - Планируется
- ⚠️ CI/CD (GitHub Actions) - Планируется

**Команды:**
```bash
pytest tests/                    # Запуск тестов
pytest --cov=src tests/          # С покрытием
```

---

## 📈 МЕТРИКИ СЛОЖНОСТИ

### Цикломатическая сложность

| Модуль | Средняя | Максимальная | Оценка |
|--------|---------|--------------|--------|
| API клиент | 8-12 | 25 | ⚠️ Средняя |
| Анализ данных | 6-10 | 18 | ✅ Низкая-средняя |
| База данных | 5-8 | 15 | ✅ Низкая |
| Визуализация | 4-7 | 12 | ✅ Низкая |

### Качество кода

| Метрика | Значение | Оценка |
|---------|----------|--------|
| Средняя длина функции | 25-30 строк | ✅ Хорошо |
| Максимальная длина функции | ~150 строк | ⚠️ Требует рефакторинга |
| Глубина вложенности | 2-3 уровня | ✅ Хорошо |
| Дублирование кода | ~5-10% | ✅ Низкое |

---

## 🔍 СТАТИЧЕСКИЕ АНАЛИЗАТОРЫ

**Используемые инструменты:**
- ⚠️ `pylint` - Анализ качества (планируется)
- ⚠️ `flake8` - PEP 8 проверка (планируется)
- ⚠️ `mypy` - Статическая типизация (планируется)
- ⚠️ `bandit` - Безопасность (планируется)

**Рекомендуемые проверки:**
```bash
flake8 src/ analysis_modules/     # Стиль кода
mypy src/ analysis_modules/        # Типизация
bandit -r src/                    # Безопасность
pylint src/ analysis_modules/     # Качество
```

**Известные проблемы:**
1. ⚠️ Отсутствие type hints в некоторых модулях
2. ⚠️ Некоторые функции превышают рекомендуемую длину
3. ✅ Код следует основным принципам PEP 8

---

## 🗄️ СТРУКТУРЫ ХРАНЕНИЯ ДАННЫХ В БД

### Архитектура базы данных

**Тип БД:** SQLite 3

**Объем:**
- Размер файла: ~500-800 MB
- Записей: 1,179,419
- Уникальных вакансий: 218,788

### Таблицы

1. **`vacancies`** - Основная таблица (58 полей, 12 индексов)
2. **`skills`** - Нормализованные навыки (Many-to-Many)
3. **`regions`** - Агрегированные данные по регионам
4. **`industry_segments`** - Агрегированные показатели по отраслям
5. **`time_series`** - Временные ряды для динамики

**Всего индексов:** 20 (12 простых + 8 композитных)

---

## ❓ ПОЧЕМУ JSON В SQLITE?

### Обоснование

#### 1. **Гибкость структуры**
- Навыки приходят как массив с переменной структурой
- Количество навыков варьируется (0-20+)
- JSON позволяет сохранить полную структуру без изменения схемы

#### 2. **Производительность при записи**
- **Нормализация:** 1 INSERT в vacancies + N INSERT в skills = 1+N операций
- **JSON:** 1 INSERT в vacancies = 1 операция
- **Выигрыш:** В N раз меньше операций

#### 3. **Совместимость с исходными данными**
- Данные из API в формате JSON
- Сохранение исходной структуры
- Легко восстановить исходные данные

#### 4. **SQLite поддерживает JSON**
- Встроенные функции: `json_extract()`, `json_array_length()`
- Эффективные запросы к JSON данным
- Индексы на JSON поля (через генеративные колонки)

#### 5. **Двойное хранение (JSON + нормализация)**
- **`key_skills_json`** - для быстрой загрузки и сохранения структуры
- **Таблица `skills`** - для эффективного анализа и поиска

**Преимущества:**
- ✅ Быстрая загрузка (JSON)
- ✅ Эффективный анализ (нормализация)
- ✅ Сохранение структуры (JSON)
- ✅ Гибкость (оба подхода)

---

## 📊 ER-ДИАГРАММА

### Связи между таблицами

```
vacancies (1) ←→ (Many) skills
    ↓
    └── vacancy_id → vacancies.id

vacancies → regions (по полю region)
vacancies → industry_segments (по полю industry_segment)
```

**Полная ER-диаграмма:** `docs/database_model.puml`

---

## 🖼️ UML-ДИАГРАММА И ВИД ДАТАСЕТА

- UML/ER: `docs/database_model.puml` → PNG для слайда: `docs/database_model.png`
- Скрин вида датасета: `docs/dataset_view_sample.png` (образец вакансий + топ-10 навыков)
- HTML-представление для подробного просмотра: `docs/dataset_view.html`
- Автогенерация артефактов:
  ```bash
  python scripts/generate_presentation_assets.py
  ```

![UML-диаграмма БД](database_model.png)

![Вид датасета (образец)](dataset_view_sample.png)

---

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Технологический стек

| Компонент | Технология |
|-----------|------------|
| Язык | Python 3.11+ |
| БД | SQLite 3 |
| HTTP | aiohttp (асинхронный) |
| Данные | pandas 2.x |
| Визуализация | matplotlib, seaborn 3.x |
| Статистика | numpy, scipy 1.x |

### Производительность

- **Сбор данных:** ~300K вакансий/неделю за 23 минуты
- **Анализ данных:** ~1-2 минуты для 1.2M записей
- **Генерация графиков:** ~30-60 секунд для 9 графиков

### Архитектура

- **Модульная архитектура** - четкое разделение на модули
- **Repository pattern** - для работы с БД
- **Асинхронные запросы** - до 25 параллельных
- **Индексы в БД** - для быстрых запросов

---

## 📋 КРАТКАЯ СВОДКА ДЛЯ СЛАЙДА

### Что добавить в презентацию:

1. **KLOC:** ~5.7 тысяч строк кода (основной код)
2. **Покрытие тестами:** ~25% (требует улучшения до 70%+)
3. **Автоматизация:** pytest, планируется CI/CD
4. **Метрики сложности:** Средняя цикломатическая сложность 6-12
5. **Статические анализаторы:** Планируется (pylint, flake8, mypy)
6. **Структуры БД:** 5 таблиц, 20 индексов, ER-диаграмма
7. **JSON в SQLite:** Гибкость, производительность, совместимость
8. **Технические детали:** Асинхронность, модульность, оптимизация

**Полная документация:** `docs/SOFTWARE_TECHNICAL_SPECIFICATIONS.md`

