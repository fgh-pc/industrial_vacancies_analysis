# ДОКУМЕНТАЦИЯ ПО МОДЕЛИ ДАННЫХ

## 📁 Файлы документации

1. **`DATABASE_MODEL.md`** - Полное описание модели данных
2. **`database_model.puml`** - UML-диаграмма в формате PlantUML

## 📊 Характеристики датасета

| Параметр | Значение |
|----------|----------|
| Анализируемый период | 2 месяца |
| Еженедельный запрос к API hh.ru | ~5,000 запросов |
| Всего запросов к API | ~50,000 |
| Еженедельный сбор вакансий | ~300,000 |
| Всего собрано вакансий | ~3,000,000 |
| Отфильтровано | ~2,781,212 |
| **Объем БД** | **1,179,419 записей** |
| Время сбора (за неделю) | 23 минуты |
| **Уникальных вакансий** | **218,788** |

## 🗄️ Структура базы данных

База данных содержит 5 основных таблиц:

1. **`vacancies`** - Основная таблица вакансий (~1.2M записей)
2. **`skills`** - Нормализованная таблица навыков
3. **`regions`** - Региональная аналитика
4. **`industry_segments`** - Отраслевые сегменты
5. **`time_series`** - Временные ряды для динамики

## 🔍 Просмотр данных

### Использование скриптов:

```bash
# Показать примеры данных
python scripts/show_database_sample.py

# Показать структуру схемы (если доступна база)
python scripts/view_database_schema.py
```

### Использование Python:

```python
import sqlite3

conn = sqlite3.connect("industrial_vacancies.db")
conn.row_factory = sqlite3.Row
cursor = conn.cursor()

# Пример запроса
cursor.execute("SELECT * FROM vacancies WHERE is_industrial = 1 LIMIT 5")
rows = cursor.fetchall()

for row in rows:
    print(f"{row['name']} - {row['employer_name']}")
```

## 📐 UML-диаграмма

Для просмотра UML-диаграммы используйте PlantUML:

1. Установите PlantUML: http://plantuml.com/
2. Откройте файл `docs/database_model.puml`
3. Сгенерируйте изображение или просмотрите в редакторе

Или используйте онлайн-редактор:
- http://www.plantuml.com/plantuml/uml/
- Скопируйте содержимое `database_model.puml` и вставьте в редактор

## 🔗 Связи между таблицами

```
vacancies (1) ←→ (Many) skills
    ↓
    └── vacancy_id → vacancies.id
```

## 📝 Основные поля таблицы vacancies

- **Идентификаторы**: id, hh_id
- **Локация**: area, region
- **Зарплата**: salary_from, salary_to, salary_avg_rub
- **Классификация**: industry_segment, position_level
- **Работодатель**: employer_name, employer_id
- **Навыки**: key_skills_json (JSON), также в таблице skills
- **Временные метки**: published_at, created_at, collected_at

## 🎯 Особенности модели

1. **Нормализация навыков** - отдельная таблица для эффективного анализа
2. **Гибкая классификация** - текстовые поля для динамической категоризации
3. **Множество индексов** - оптимизация запросов
4. **Агрегированные таблицы** - ускорение аналитики

